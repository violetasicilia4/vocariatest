# Vocaria — variables de entorno
#
# Por ahora la app es una SPA estática sin backend ni IA conectada.
# No se requiere ninguna variable para correr el proyecto localmente.
#
# Cuando se implemente el informe con IA, la API key NO debe ir acá
# (se expondría en el browser): debe vivir en un backend/serverless
# (ej: Netlify Function) que llame a la API del lado del servidor.
